name: Build and Push image to ACR
on:
    push:
        branches:
            - main
jobs:
    build-and-push:
        runs-on: ubuntu-latest
        steps:
            - name: checkout the code
              uses: actions/checkout@v2
            - name: Azure Login
              uses: azure/login@v2
              with:
                creds: ${{secrets.AZURE_CREDENTIALS}}
            - name: ACR login
              run: |
                az acr login --name mydockerregistry3
            - name: Build and push the image
              run: |
                
                docker build -t mydockerregistry3.azurecr.io/myapp:latest .
                docker push mydockerregistry3.azurecr.io/myapp:latest